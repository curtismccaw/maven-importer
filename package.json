{
  "name": "maven-importer-backend",
  "version": "0.1.0",
  "description": "Server-side product import pipeline for Maven's six-brand Shopify catalogue import (replaces the browser-artifact chat-handoff mechanism).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd frontend && npm install && npm run build",
    "test": "node --test test/"
  },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "multer": "^2.0.0",
    "papaparse": "^5.4.1",
    "pdf-parse": "^1.1.1",
    "xlsx": "^0.18.5"
  }
}
